@inherits LayoutComponentBase
@layout User.Client.Layout.MainLayout
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/items", replace: true);
    }
}
