@page "/"
@inject NavigationManager NavigationManager

<PageTitle>優待商品マスタ一覧</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/reward-items", replace: true);
    }
}
